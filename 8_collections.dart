enum colors {blue,red}  //They must be defined outside of the main fuction
void main(List<String> args) {
  print(colors); //colors
  print(colors.values); //colors.blue,colors.red
  print(colors.blue); //colors.blue
}