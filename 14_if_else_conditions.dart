//the normal if and else statements of the whole live
void main(List<String> args) {
  var age = 51;
  if (age > 50) {
    if (age > 100) {
      print("do you are alive yet?");
    }
    if (age < 80) {
      print("you are proxmate to die (probably)");
    }
  }else{
    print("you have at least the half of your live, enjoy it");
  }
}
